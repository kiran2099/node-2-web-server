<!doctype html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>Website</title>
    </head>

    <body>
        <h1>{{toUpperCase pageTitle}}</h1>
        {{> header}}
        {{> footer}}
    </body>
</html>